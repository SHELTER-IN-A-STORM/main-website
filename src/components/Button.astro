---
interface Props {
    href: string;
    variant?: 'primary' | 'secondary' | 'brand';
    size?: 'sm' | 'md' | 'lg';
    class?: string;
}

const { 
    href, 
    variant = 'secondary',
    size = 'md',
    class: className = ''
} = Astro.props;

const sizes = {
    sm: 'px-4 py-2 text-sm',
    md: 'px-6 py-3 text-base',
    lg: 'px-8 py-4 text-lg'
};

const variants = {
    primary: 'text-white bg-brand hover:bg-brand-dark',
    secondary: 'text-white bg-white/10 hover:bg-white/20',
    brand: 'text-white bg-brand hover:bg-brand-dark'
};
---

<a
    href={href}
    class:list={[
        'inline-flex items-center font-medium rounded-lg transition-colors duration-300',
        sizes[size],
        variants[variant],
        className
    ]}
>
    <slot />
</a> 